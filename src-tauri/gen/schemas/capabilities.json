{"default":{"identifier":"default","description":"Permissions for the main window. The harness reaches the filesystem and processes through Rust, not through frontend plugins, so the webview needs almost nothing.","local":true,"windows":["main"],"permissions":["core:default","dialog:allow-open","opener:allow-open-url"]}}